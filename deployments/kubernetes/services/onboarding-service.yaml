apiVersion: v1
kind: Service
metadata:
  name: onboarding-service
  namespace: telcobss
spec:
  selector:
    app: onboarding-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
